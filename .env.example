# Django Settings
SECRET_KEY=your-secret-key-here-generate-new-one
DEBUG=False
ALLOWED_HOSTS=localhost,127.0.0.1

# ============================================================================
# DATABASE CONFIGURATION
# ============================================================================
# Neon PostgreSQL (for production)
# DATABASE_URL=postgresql://user:password@host:port/database

# ============================================================================
# CORS & CSRF (for API and security)
# ============================================================================
# Add your Render deployment domain
# Example: https://waterworks-sos3.onrender.com
CORS_ALLOWED_ORIGINS=https://waterworks-sos3.onrender.com
CSRF_TRUSTED_ORIGINS=https://waterworks-sos3.onrender.com

# ============================================================================
# SMART METER IOT INTEGRATION
# ============================================================================
# Generate a strong random key for smart meter webhook authentication
# Example: python -c "import secrets; print(secrets.token_urlsafe(32))"
SMART_METER_API_KEY=your-smart-meter-api-key-here

# ============================================================================
# EMAIL CONFIGURATION (Gmail SMTP for Password Reset) - REQUIRED
# ============================================================================
# IMPORTANT: You MUST set up a Gmail App Password for email functionality
#
# Setup Instructions:
# 1. Go to your Google Account: https://myaccount.google.com/
# 2. Navigate to Security -> 2-Step Verification (enable if not already enabled)
# 3. Go back to Security -> App passwords
# 4. Select app: Mail, Select device: Other (Custom name)
# 5. Enter "Balilihan Waterworks" as the app name
# 6. Click Generate
# 7. Copy the 16-character app password (no spaces)
# 8. Paste it below as EMAIL_HOST_PASSWORD
#
# DO NOT use your regular Gmail password - it won't work!
# Use a dedicated Gmail account for the system (e.g., balilihanwaterworks@gmail.com)

EMAIL_HOST_USER=your-gmail-address@gmail.com
EMAIL_HOST_PASSWORD=your-16-character-app-password-here
DEFAULT_FROM_EMAIL=Balilihan Waterworks <noreply@balilihan-waterworks.com>

# ============================================================================
# CLOUDINARY CONFIGURATION (For Mobile App Image Uploads)
# ============================================================================
# To allow Field Staff to take pictures of water meters and upload them,
# create a free account at https://cloudinary.com and enter your keys below:
# CLOUDINARY_CLOUD_NAME=your_cloud_name
# CLOUDINARY_API_KEY=your_api_key
# CLOUDINARY_API_SECRET=your_api_secret

# ============================================================================
# RENDER ENVIRONMENT VARIABLES (auto-set by Render)
# ============================================================================
# These are automatically provided by Render - do NOT set manually:
# RENDER=true
# RENDER_EXTERNAL_URL=https://your-app-name.onrender.com
